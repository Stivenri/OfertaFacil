document.getElementById("registerForm").addEventListener("submit", function(event) {
    event.preventDefault();
    alert("Formulario enviado");
    // Aquí puedes agregar lógica para conectarte a la base de datos
});
